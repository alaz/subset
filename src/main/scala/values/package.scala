package com.osinka.subset

/** Collection of [[com.osinka.subset.ValueReader]] and [[com.osinka.subset.ValueWriter]] implicits.
  *
  * It's split over several traits, which are commonly imported via
  * `SmartValues` or `StrictValues`,
  * `Extractors` helper object and
  * `Joda` for optional inclusion.
  */
package object values